console.log("Kelvin Fon resume portfolio loaded successfully.");

document.addEventListener("DOMContentLoaded", () => {
  const currentYear = new Date().getFullYear();
  console.log(`Site active - ${currentYear}`);
});